# Override service ports when running docker compose
GATEWAY_PORT=3010
REPORTING_PORT=3001
MATCHING_PORT=3002
NOTIFICATION_PORT=3003

# Database/Redis host ports are already non-default to avoid conflicts
# Postgres -> 5433, Redis -> 6380
